<h1><%= @user[:username] %></h1>
<h2>Comments:</h2>
<ul>
  <% @comments.each do |comment| %>
    <li>
    <strong><%= link_to "#{comment.movie.title}", comment.movie %>:</strong>
    <%= comment.text %>
    </li>
  <% end %>
</ul>